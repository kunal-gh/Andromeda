# ── Heavyweight Machine Learning Dependencies ─────────────────────
# These dependencies are installed on AWS Fargate but ignored on Vercel
# to stay under the 250MB Lambda bundle limit.

qdrant-client>=1.9.0
sentence-transformers>=3.0.0
langchain-qdrant>=0.1.0
langchain-community>=0.3.0
networkx>=3.0.0
